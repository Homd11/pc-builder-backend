PORT=5000
CORS_ORIGIN=*
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb+srv://your_user:your_password@cluster.mongodb.net/pc-builder?retryWrites=true&w=majority

# JWT Secret (used for auth tokens — pick a strong random string)
JWT_SECRET=your_jwt_secret_here
