PORT=5000
CORS_ORIGIN=*
NODE_ENV=development

# MongoDB
MONGODB_URI=your_mongodb_connection_string_here

# JWT Secret (used for auth tokens — pick a strong random string)
JWT_SECRET=your_jwt_secret_here
